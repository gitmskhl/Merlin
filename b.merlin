
def swap(arr, i, j) {
    var tmp = arr[i];
    arr[i] = arr[j];
    arr[j] = tmp;
}

def sort(arr) {
    for (var i = 0, end = len(arr); i < end; i += 1) {
        var j = i;
        while (j > 0 and arr[j] < arr[j - 1]) {
            swap(arr, j, j - 1);
            j -= 1;
        }
    }
}
