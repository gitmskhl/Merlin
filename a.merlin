
// [l, r)
def bin_search(arr, key, l, r) {
    while (r - l > 1) {
        var mid = int((r + l) / 2);
        if (arr[mid] > key) r = mid;
        else l = mid;
    }

    return l;
}

var n = 10000;

println(bin_search(range(n), 7, 0, n));